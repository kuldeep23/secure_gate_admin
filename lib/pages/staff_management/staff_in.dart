import 'package:flutter/material.dart';

class StaffIn extends StatelessWidget {
  const StaffIn({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('StaffIn')));
  }
}
