import 'package:flutter/material.dart';

class CarOut extends StatelessWidget {
  const CarOut({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('CarOut')));
  }
}
